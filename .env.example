# Copy this file to .env for local testing with `vercel dev`.
# In production (Vercel dashboard), set this as a real Environment Variable —
# never commit your actual key to a file or a git repo.

ANTHROPIC_API_KEY=your_key_here
